package day25_arraylist_continued;

public class ArrayList_Recap {
	
	/*
	 * 
	 * ArrayList - is a part of Collection Interface, it can store multiple values, it is re-sizable.
	 * ArrayList can store only non-primitive data types. Elements within the ArrayList are indexed
	 * starting with 0.
	 * 
	 *  0  1  2  3  4    - index
	 * [1, 4, 6, 8, 6]
	 * 
	 * ArrayList: 
	 * 	- add: just adds the value to the end of the ArrayList, unless a specific index is given;
	 * 	- remove: deletes the specified element;
	 * 	- set: changes the existing value of the element under the given index with the value
	 * that is provided by us;
	 * 	- size: gives us a total number elements within this ArrayList;
	 * 	- get: gives us the element that is located at a prticular index; 
	 * 	- contains: returns either true or flase based on if the ArrayList contains the provided value
	 * 	- we can store different data types within the ArrayList;
	 * 
	 * 		for (Object obj : arrayList) {
	 * 			System.out.println("Element " + obj);
	 * 	}
	 * 
	 * int num = array.length;    - array
	 * int num1 = text.length();  - String
	 * 
	 * 
	 * Warmup Task:
	 * 
	 * Create class Holidays
	 * private static list holidays
	 * no-arg constructor that will add 6 holidays to the list
	 * static method isHoliday that accepts a string and checks if that holiday is in the list of the holiday.
	 */

}
