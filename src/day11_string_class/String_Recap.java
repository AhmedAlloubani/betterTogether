package day11_string_class;

public class String_Recap {
	
	public static void main(String[] args) {
		
		/*
		 * 1. String is a non primitive data type (object) that represents a character sequence.
		 * "Hello, World!"
		 * 
		 * - String literal creation: String str1 = "Hello";
		 * - Object type creation (new): String str2 = new String("Good bye");
		 * 
		 * String literal creation places the object in the String pool (located inside java heap)
		 * Object type creation places the object inside the heap itself. 
		 * 
		 * String so = "Text;
		 * String brother = "Text;
		 * son == brother // true
		 * 
		 * String str3 =  new String ("Good bye");
		 * String str4 = new String ("Good bye");
		 * str3 == str4 // false
		 * 
		 * == - compare the memory slots by the objects
		 * 
		 * 2. Strings are immutable (strings can't be changed after being created)
		 * every time we want to change the string, the new string will be created and the old one would be destroyed.
		 * 
		 * 3. Strings are a char sequence 
		 * String str = "Hello";
		 */ // 			01234   in programming we always start counting with 0.
		 
		
	}

}
