package day24_arraylist;

public class Arrays_Recap {
	
	/*
	 * Arrays - collection of data;
	 * - arrays can only contain one data type;
	 * - arrays are fixed size and can't be changed once they are declared;
	 * - we can still change the elements within an array;
	 * - boolean[] array = new boolean[5]; - default values will be given to the elements
	 * 	 boolean array[] - will compile, but is not recommended
	 * 	 print(array[0]); // false
	 * 	 array[0] = true;
	 * 	 print(array[0]); // true
	 * - boolean[] array = {true, false, true};
	 * - boolean[] array = new boolean[] {true, true, true};
	 * - System.out.println("Today is " + days[1]); - retrieve the value from array
	 * - length is used to find the number of elements in the array
	 * 
	 * - enhanced for loop or simply for-each loop: for(String text : texts) {}
	 * - public int[] prinArray(int[] array) {
	 * 		...
	 *      return nums;
	 *   }
	 *   
	 * - Arrays Class: this class was specifically designed to help programmers to work with arrays
	 * 	- contains only static methods for convenience 
	 * 		- sort(); - going to sort lowest to highest(numbers), alphabetically (Strings);
	 * 		- toString(); - will create textual representation of the elements within the array
	 * 		- equals(); - number of elements, order and values;
	 * 		- binarySearch(); - searches for a value in the array. Gotta sort first! Returns an index
	 * 		- Clone(); - just copies the whole array
	 * 		- copyOf(); - is a more flexible method that can accept a length
	 * 
	 * - split(""): chops a given string using the provided delimiter
	 * - tpCharArray(): "Hello" -> 'H', 'e', 'l' , 'l', 'o'
	 * 
	 * Task 1:
	 *  - create a class Animal;
	 *  - private instance variable name;
	 *  - constructor that accepts a name and assigns it to the instance variable;
	 *  - getters and setters;
	 *  
	 *  - create another class name Zoo;
	 *  - create an array that contains 5 different animals;
	 *  - iterate over this array and print each animal's name;
	 *   
	 *   
	 */

}
